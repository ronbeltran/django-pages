============
Installation
============

At the command line::

    $ easy_install django-pages

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pages
    $ pip install django-pages
